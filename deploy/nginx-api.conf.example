# 复制到服务器 /etc/nginx/sites-available/ （例如 api.conf），启用 HTTPS 后把 server_name、证书路径改成你的域名。
# 假设 API 由本机 Docker 映射到 127.0.0.1:3000。

upstream lims_api {
    server 127.0.0.1:3000;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name www.xinde8888.com xinde8888.com;

    # Certbot 会自动写入 rewrite；也可手动：
    # return 301 https://$host$request_uri;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://lims_api;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
        client_max_body_size 50m;
    }
}

# HTTPS 示例（certbot --nginx 会自动生成类似块）
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name www.xinde8888.com xinde8888.com;
#     ssl_certificate /etc/letsencrypt/live/www.xinde8888.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/www.xinde8888.com/privkey.pem;
#     location / {
#         proxy_pass http://lims_api;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         client_max_body_size 50m;
#     }
# }
